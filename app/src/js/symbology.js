/* eslint-env browser  */
console.log("init sym");
const height = 50,
    width = 50,
    colorr = "red";
    
        
    
            var svg7 = d3.select("#rectangle")
            .append("svg")
            .attr("height", height)
            .attr("width", width)
            .style("fill", colorr);
            
